# AIMternet-Cafe

Data engineering POC for an internet cafe: membership, PC rentals, concessions, workstation
telemetry. Raw files on EC2 flow through Airflow into S3 Bronze, are validated and normalized,
load into RDS PostgreSQL (operational) and DynamoDB (events + telemetry), are curated into S3
Silver/Gold Parquet, and land in Redshift as a dimensional model. A FastAPI operational API is
the only write path for the Jupyter POS terminal; a metrics API feeds an HTML/JS dashboard.

```
EC2 raw landing (read-only)
        │
        ▼
     Airflow ──────────────► S3 Bronze (immutable)
                                 │
                    ┌────────────┼────────────┐
                    ▼            ▼            ▼
              validate/     DynamoDB     quarantine/
              normalize    (events,       rejects
                    │      telemetry)
        ┌───────────┴───────────┐
        ▼                       ▼
  RDS PostgreSQL          S3 Silver → S3 Gold
  (operational)                        │
                                       ▼
                                   Redshift

Jupyter POS ──HTTP──► Operational API ──► RDS + DynamoDB
Dashboard   ──HTTP──► Metrics API      ──► Redshift + DynamoDB
```

## Data

62 days, 2026-07-01 → 2026-08-31, ~4.2 GB in `data/raw-landing/` (read-only):

| Dataset | Rows |
|---|---:|
| `catalog/workstations.csv` | 175 |
| `catalog/concession_items.csv` | 10 |
| `dimensions/dim_date.csv` / `dim_time.csv` | 365 / 1,440 |
| `members.csv` (all batches) | 360 |
| `rental_transactions.csv` | 28,287 |
| `concession_purchases.csv` | 21,077 |
| `concession_order_items.csv` | 29,672 |
| `member_points_ledger.csv` | 55,514 |
| `workstation_events.json` | 58,218 |
| `telemetry/**/*.json` | **6,300,000** |

The telemetry figure is measured, not projected. The spec's ~3,124,800 assumes a uniform
5-minute tick; the tick drops to 30 seconds on 2026-08-25 (finding **F6**), which doubles the
real count and the DynamoDB bill with it.

## Quick start

```bash
cp .env.example .env      # then fill in the real values
make env                  # conda env + editable install
make link                 # create /opt/aimternet and /opt/airflow (needs sudo)
make check                # lint + typecheck + test
make validate             # validate all 62 batches locally — no AWS needed
```

Then load it:

```bash
make migrate              # schema aimternet_oltp
make bootstrap            # landing -> S3 Bronze -> validate -> RDS + DynamoDB
make curate               # Silver -> RDS export -> Gold
make redshift             # Gold -> aimternet_olap
make reconcile            # the report that says whether all of that was true
make api                  # :8000, dashboard at /dashboard
make airflow              # :8080
```

`docs/runbook.md` covers day-to-day operation and the failure modes worth knowing about.

## Pipeline

| DAG | Schedule | Hop |
|---|---|---|
| `bootstrap_raw_landing` | manual | EC2 landing -> Bronze -> RDS + DynamoDB. The only DAG that reads the landing directory |
| `rds_to_s3_incremental` | `0 * * * *` | RDS -> Silver operational snapshots |
| `dynamodb_to_s3_incremental` | `15 * * * *` | API-emitted events -> Silver snapshot -> Gold |
| `curate_silver_gold` | `30 * * * *` | Bronze -> Silver -> Gold |
| `load_redshift` | `45 * * * *` | Gold -> Redshift |
| `reconcile_data` | `0 */6 * * *` | Every layer against every other |

DAG files hold no business logic — they import and call `src/aimternet/`, and take their
tuning from Airflow Variables so `notebooks/airflow_lens.ipynb` can change behaviour without
a deploy.

## APIs

`make api` serves both on `:8000`. Neither has an arbitrary-SQL endpoint.

**Operational** — the only write path into RDS. `GET /v1/workstations/available`,
`GET /v1/members/{id}`, `POST /v1/rentals/check-in`, `POST /v1/rentals/check-out`,
`GET /v1/rentals/active`, `GET /v1/concessions/catalog`, `POST /v1/concessions/purchases`.
Writes take an `Idempotency-Key`; failures come back as RFC 7807 problem documents.

**Metrics** — read-only, under `/v1/metrics`: `summary`, `revenue/today`, `revenue/by-zone`,
`points`, `workstations/status`, `rentals/active`, `utilization/hourly`, `telemetry/recent`.
Live numbers from RDS, historical aggregates from Redshift, status and telemetry from
DynamoDB.

## Documentation

| Document | What it holds |
|---|---|
| `CLAUDE.md` | Stack, the eight invariants, and the data findings that shape the code |
| `docs/runbook.md` | Running it, and what to do when a layer disagrees with another |
| `docs/assumptions.md` | Every policy decision with its evidence — generated by `make docs` |
| `docs/dynamodb.md` | The key design, written before the loader as §6.3 requires |
| `infra/README.md` | What Terraform manages, what it deliberately cannot, and the costs |
| `pipeline_plan_AWS.md` | The authoritative build specification |

## Notebooks

| Notebook | What it is | Prerequisite |
|---|---|---|
| `notebooks/pos_terminal.ipynb` | Front-desk POS — check members in/out, sell concessions. HTTP to the API only, no DB access, no credentials (invariant #3). | `make api` running. |
| `notebooks/airflow_lens.ipynb` | Observe and control DAGs, runs, tasks, Variables and pools via the Airflow REST API — no DAG source edits. | `make airflow` running as `standalone` (api-server alone shows scheduler/triggerer as `unhealthy`). |
| `notebooks/db_lens.ipynb` | Read-only explorer over RDS (`aimternet_oltp`) and Redshift (`aimternet_olap`), via the `aimternet_ro` role in a `READ ONLY` transaction. | RDS migrated (`make migrate` / `make bootstrap`) for the OLTP half; `make redshift` for the OLAP half. No service needs to be running — it connects to the databases directly. |
| `notebooks/dashboard_lens.ipynb` | Exercises every `/v1/metrics/*` endpoint and links to the live dashboard. | `make api` running, with data in RDS, Redshift and DynamoDB to get meaningful numbers (`make bootstrap` + `make curate` + `make redshift`). |

Typical order: `make bootstrap` (or trigger it from `airflow_lens.ipynb` once `make airflow` is up) →
`make api` for `pos_terminal.ipynb` and the OLTP half of `db_lens.ipynb` → `make curate` +
`make redshift` → the OLAP half of `db_lens.ipynb` and `dashboard_lens.ipynb`.

## Cost

Measured 2026-09-03, us-east-1 on-demand.

| | Monthly |
|---|---:|
| S3 Standard, 4.51 GB across all five prefixes | $0.10 |
| DynamoDB storage, 2.93 GB | $0.73 |
| **Managed by this build** | **$0.83** |
| RDS `db.t3.micro` + 20 GB gp3 — *pre-existing, shared* | ~$14.70 |
| Redshift `dc2.large`, 1 node — *pre-existing, shared* | **~$180** |

Plus one one-off charge already incurred: **~$8.09** of DynamoDB write units for the initial
load (6.3M telemetry items at 1 WRU, 58k event items at 3 WRU). That is double the spec's
projection because the record count in the spec is wrong — see F6 above.

Redshift is the bill. This build neither creates nor destroys it, and `infra/` deliberately
cannot: see `infra/README.md`.
